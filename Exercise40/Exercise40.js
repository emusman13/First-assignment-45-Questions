function make_album (name, title, track = 0)
{

    let albumDic = {
        name: name,
        title: title,
    }
//your logic fir tracks
    return albumDic

}

const musicAlbum = make_album("asif", "title", 5) // 


